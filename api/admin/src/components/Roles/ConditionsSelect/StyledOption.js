import styled from 'styled-components';
import { Option } from '@buffetjs/core';

const StyledOption = styled(Option)`
  > span {
    display: block !important;
    color: #6d7d7b !important;
  }
`;

export default StyledOption;
